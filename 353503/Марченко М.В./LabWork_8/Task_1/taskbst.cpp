#include "taskbst.h"

/* *TaskBST::remove(Node *root, int key) {

    if (root == nullptr) {
        return root;
    }
    if (key < root->data.key) {
        root->left = remove(root->left, key);
    } else if (key > root->data.key) {
        root->right = remove(root->right, key);
    } else {
        clear(root);
        return nullptr;
    }
    return root;
}*/
